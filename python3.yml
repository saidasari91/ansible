---
- name: installing python package
  hosts: all
  become: true

  tasks:
    - name: pip install
      ansible.builtin.dnf:
        name: python3-pip
        state: present

    - name: panda install
      ansible.builtin.pip:
        name: pandas
        executable: pip3   
